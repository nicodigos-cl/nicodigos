import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
export default function CommunicationsSettingsPage() { const integrations = [{ name: "Resend", connected: Boolean(process.env.RESEND_API_KEY && process.env.RESEND_FROM && process.env.RESEND_WEBHOOK_SECRET), details: "Envío, recepción y eventos de entrega", route: "/api/webhooks/resend" }, { name: "OneSignal", connected: Boolean(process.env.ONESIGNAL_APP_ID && process.env.ONESIGNAL_REST_API_KEY && process.env.NEXT_PUBLIC_ONESIGNAL_APP_ID), details: "Web SDK y API de notificaciones", route: "/push/onesignal/OneSignalSDKWorker.js" }, { name: "Jobs de comunicaciones", connected: Boolean(process.env.CRON_SECRET), details: "Envíos en cola, programaciones y métricas", route: "/api/cron/process-communications" }]; return <div className="space-y-5"><header><h1 className="font-heading text-2xl font-semibold">Configuración</h1><p className="mt-1 text-sm text-muted-foreground">Estado seguro de proveedores, webhooks y worker. Las credenciales nunca se muestran.</p></header><div className="grid gap-3 lg:grid-cols-3">{integrations.map((integration) => <Card key={integration.name}><CardHeader><div className="flex items-center justify-between gap-2"><CardTitle className="text-base">{integration.name}</CardTitle><Badge variant={integration.connected ? "secondary" : "destructive"}>{integration.connected ? "Conectado" : "Sin configurar"}</Badge></div></CardHeader><CardContent className="space-y-2 text-sm"><p className="text-muted-foreground">{integration.details}</p><code className="block break-all rounded bg-muted p-2 text-xs">{integration.route}</code></CardContent></Card>)}</div><Card><CardHeader><CardTitle className="text-base">Configuración externa requerida</CardTitle></CardHeader><CardContent className="space-y-3 text-sm text-muted-foreground"><p>En Resend configura un dominio receptor o subdominio con MX y registra el webhook público para <code>email.received</code>, <code>email.sent</code>, <code>email.delivered</code>, <code>email.delivery_delayed</code>, <code>email.bounced</code>, <code>email.complained</code> y <code>email.failed</code>.</p><p>En OneSignal configura el mismo origen de producción y el worker dedicado en <code>/push/onesignal/OneSignalSDKWorker.js</code> con scope <code>/push/onesignal/</code>. HTTPS es obligatorio fuera de localhost.</p><p>Programa el cron de comunicaciones al menos cada minuto. La interfaz usa America/Santiago; la base de datos conserva timestamps normalizados.</p></CardContent></Card></div>; }
