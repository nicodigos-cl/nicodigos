/** App display and settlement currency. */
export const APP_CURRENCY = "CLP" as const;

/** Kinguin API prices are quoted in EUR. */
export const KINGUIN_SOURCE_CURRENCY = "EUR" as const;

export const DEFAULT_MARKUP = 1.15;
