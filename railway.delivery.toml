[build]
builder = "RAILPACK"
buildCommand = "bunx --bun prisma generate"

[deploy]
startCommand = "bun --conditions=react-server scripts/delivery-worker.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
