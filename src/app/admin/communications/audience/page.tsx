import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { getAudienceMetrics, getAudienceSegments } from "@/lib/communications/audience-queries";
export default async function AudiencePage() { const [metrics, segments] = await Promise.all([getAudienceMetrics(), getAudienceSegments()]); const cards = [["Usuarios totales", metrics.totalUsers], ["Emails válidos", metrics.validEmails], ["Consentimiento marketing", metrics.marketingConsent], ["Dados de baja", metrics.optedOut], ["Suscriptores web activos", metrics.activePush], ["Permiso web bloqueado", metrics.deniedPush], ["Suscriptores anónimos", metrics.anonymousPush], ["Dispositivos vinculados", metrics.linkedPush]] as const; return <div className="space-y-5"><header><h1 className="font-heading text-2xl font-semibold">Audiencia</h1><p className="mt-1 text-sm text-muted-foreground">Elegibilidad, consentimiento y dispositivos sin exponer tokens ni listas completas de emails.</p></header><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([label, value]) => <Card key={label}><CardContent className="p-4"><p className="text-xs text-muted-foreground">{label}</p><p className="mt-1 text-2xl font-semibold">{value}</p></CardContent></Card>)}</div><Card><CardHeader><CardTitle className="text-base">Segmentos internos</CardTitle></CardHeader><CardContent>{segments.length ? <ul className="divide-y divide-border">{segments.map((segment) => <li key={segment.id} className="flex items-center justify-between gap-4 py-3"><div><p className="text-sm font-medium">{segment.name}</p><p className="text-xs text-muted-foreground">{segment.description ?? "Sin descripción"}</p></div><p className="text-sm tabular-nums">{segment.estimatedRecipients ?? "Por estimar"} <span className="text-xs text-muted-foreground">elegibles</span></p></li>)}</ul> : <p className="py-8 text-center text-sm text-muted-foreground">No hay segmentos guardados. Las audiencias ad hoc se resuelven desde el formulario de envío.</p>}</CardContent></Card><p className="text-xs text-muted-foreground">{metrics.deviceCount} dispositivos registrados entre {metrics.usersWithDevices} usuarios. Los identificadores completos de suscripción no se muestran.</p></div>; }
