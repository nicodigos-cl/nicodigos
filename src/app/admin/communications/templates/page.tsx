import { HiOutlineRectangleStack as HiOutlineTemplate } from "react-icons/hi2";
import { TemplateEditor } from "@/components/admin/communications/template-editor";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent } from "@/components/ui/card";
import { Empty, EmptyDescription, EmptyHeader, EmptyMedia, EmptyTitle } from "@/components/ui/empty";
import { getCommunicationTemplates } from "@/lib/communications/template-queries";
export default async function TemplatesPage() { const templates = await getCommunicationTemplates(); return <div className="space-y-5"><header className="flex items-start justify-between gap-4"><div><h1 className="font-heading text-2xl font-semibold">Plantillas</h1><p className="mt-1 text-sm text-muted-foreground">Versiones inmutables para email y notificaciones web.</p></div><TemplateEditor /></header>{templates.length ? <div className="grid gap-3 md:grid-cols-2">{templates.map((template) => <Card key={template.id}><CardContent className="p-5"><div className="flex items-start justify-between gap-3"><div><h2 className="font-medium">{template.name}</h2><p className="text-xs text-muted-foreground">{template.slug}</p></div><div className="flex gap-1"><Badge variant="outline">{template.channel === "EMAIL" ? "Email" : "Web push"}</Badge><Badge variant="secondary">{template.status === "DRAFT" ? "Borrador" : template.status === "ACTIVE" ? "Activa" : "Archivada"}</Badge></div></div><p className="mt-3 line-clamp-3 text-sm text-muted-foreground">{template.latest?.subject ?? template.latest?.title ?? template.latest?.content}</p><div className="mt-4 flex justify-between text-xs text-muted-foreground"><span>Versión {template.currentVersion}</span><span>{template.updatedByEmail ?? template.createdByEmail}</span></div></CardContent></Card>)}</div> : <Empty className="border border-border bg-card"><EmptyHeader><EmptyMedia variant="icon"><HiOutlineTemplate /></EmptyMedia><EmptyTitle>Sin plantillas</EmptyTitle><EmptyDescription>Crea la primera plantilla. No se cargarán ejemplos falsos.</EmptyDescription></EmptyHeader></Empty>}</div>; }
