import { HiOutlineBell } from "react-icons/hi2";
export function WebPushPreview({ title, body, iconUrl, imageUrl, actionText }: { title: string; body: string; iconUrl?: string; imageUrl?: string; actionText?: string }) { return <section aria-labelledby="push-preview-title" className="space-y-3 rounded-2xl border border-border bg-card p-5"><div><h2 id="push-preview-title" className="font-heading font-semibold">Preview aproximada</h2><p className="text-xs text-muted-foreground">La apariencia final depende del navegador y sistema operativo.</p></div><div className="overflow-hidden rounded-xl border border-border bg-background shadow-sm">{imageUrl ? <div className="aspect-[2/1] bg-muted bg-cover bg-center" style={{ backgroundImage: `url(${imageUrl})` }} role="img" aria-label="Imagen de la notificación" /> : null}<div className="flex gap-3 p-4">{iconUrl ? <div className="size-10 shrink-0 rounded-lg bg-muted bg-contain bg-center bg-no-repeat" style={{ backgroundImage: `url(${iconUrl})` }} /> : <div className="flex size-10 shrink-0 items-center justify-center rounded-lg bg-primary/10 text-primary"><HiOutlineBell /></div>}<div className="min-w-0"><p className="text-xs text-muted-foreground">nicodigos.com</p><p className="mt-1 font-semibold">{title || "Título de la notificación"}</p><p className="mt-1 text-sm text-muted-foreground">{body || "El mensaje aparecerá aquí."}</p>{actionText ? <span className="mt-3 inline-block rounded border border-border px-2 py-1 text-xs">{actionText}</span> : null}</div></div></div><div className="grid grid-cols-3 gap-2 text-center text-xs text-muted-foreground"><span>Chrome</span><span>Android</span><span>Safari</span></div></section>; }
