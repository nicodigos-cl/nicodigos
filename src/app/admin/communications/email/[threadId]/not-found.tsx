import Link from "next/link"; import { Button } from "@/components/ui/button"; import { Empty, EmptyContent, EmptyDescription, EmptyHeader, EmptyTitle } from "@/components/ui/empty";
export default function NotFound() { return <Empty className="border border-border bg-card"><EmptyHeader><EmptyTitle>Conversación no encontrada</EmptyTitle><EmptyDescription>Puede haber sido archivada, eliminada lógicamente o el enlace no es válido.</EmptyDescription></EmptyHeader><EmptyContent><Button nativeButton={false} render={<Link href="/admin/communications/email" />}>Volver a Correo</Button></EmptyContent></Empty>; }
